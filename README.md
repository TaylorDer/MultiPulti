# Мультимедийное веб-пособие "Методы оптимизации проектных решений"

Интерактивное SPA-приложение для изучения методов оптимизации, созданное на React + Vite + TypeScript.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Приложение откроется по адресу `http://localhost:3000`

### Сборка для продакшена

```bash
npm run build
```

### Просмотр собранной версии

```bash
npm run preview
```

## 📁 Структура проекта

```
MultiPulti/
├── public/                 # Статические файлы (изображения и т.д.)
├── src/
│   ├── components/         # React компоненты
│   │   ├── Layout.tsx      # Основной layout с header и sidebar
│   │   ├── Header.tsx     # Шапка приложения
│   │   ├── Sidebar.tsx    # Боковое меню с навигацией
│   │   └── ImageModal.tsx # Модальное окно для изображений
│   ├── pages/             # Страницы приложения
│   │   ├── Home.tsx       # Главная страница
│   │   ├── TOC.tsx        # Содержание
│   │   ├── Chapter.tsx    # Страница главы/раздела
│   │   ├── Playground.tsx # Интерактивный playground
│   │   ├── Glossary.tsx   # Глоссарий
│   │   └── About.tsx      # О пособии
│   ├── data/              # Данные приложения
│   │   ├── chapters.ts    # Главы и разделы (ВАШ КОНТЕНТ ЗДЕСЬ)
│   │   └── glossary.ts    # Глоссарий (ВАШ КОНТЕНТ ЗДЕСЬ)
│   ├── types/             # TypeScript типы
│   │   └── index.ts
│   ├── utils/             # Утилиты
│   │   ├── storage.ts     # Работа с localStorage
│   │   └── markdown.ts    # Рендеринг Markdown
│   ├── App.tsx            # Главный компонент с роутингом
│   ├── main.tsx           # Точка входа
│   └── index.css          # Глобальные стили
├── index.html
├── package.json
├── tsconfig.json
└── vite.config.ts
```

## 📝 Где добавлять ваш контент

### 1. Текст и структура глав

Откройте файл **`src/data/chapters.ts`** и отредактируйте массив `chapters`.

Каждая глава имеет структуру:

```typescript
{
  id: 'unique-chapter-id',        // Уникальный идентификатор
  title: 'Название главы',
  sections: [
    {
      id: 'unique-section-id',    // Уникальный идентификатор раздела
      title: 'Название раздела',
      content: `# Заголовок
      
Ваш Markdown контент здесь.

## Подзаголовок

Формулы LaTeX:
$$E = mc^2$$

Или встроенные: $x = \\frac{-b \\pm \\sqrt{b^2-4ac}}{2a}$

Код:
\`\`\`python
def example():
    return "Hello"
\`\`\`
`
    }
  ]
}
```

**Важно:**
- Используйте Markdown синтаксис для форматирования
- Формулы LaTeX: `$$...$$` для блочных, `$...$` для встроенных
- Код: оберните в тройные backticks с указанием языка
- Изображения: используйте стандартный Markdown синтаксис `![alt text](path/to/image.jpg)`

### 2. Изображения

Поместите изображения в папку **`public/images/`** (создайте её, если её нет).

Затем в Markdown используйте:

```markdown
![Описание изображения](/images/your-image.jpg)
```

Изображения автоматически открываются в модальном окне при клике.

### 3. Глоссарий

Откройте файл **`src/data/glossary.ts`** и добавьте термины:

```typescript
{
  term: 'Термин',
  definition: 'Определение термина'
}
```

## 🎨 Основные возможности

### Навигация
- Боковое меню со списком всех глав и разделов
- Поиск по содержанию
- Навигация "Назад/Вперед" между разделами
- Breadcrumbs (хлебные крошки)

### Прогресс чтения
- Автоматическое отслеживание прогресса по скроллу
- Сохранение прогресса в localStorage
- Визуальный индикатор прогресса вверху страницы

### Настройки
- Кнопки A+/A- для изменения размера шрифта
- Настройки сохраняются автоматически
- Размер шрифта применяется ко всему контенту

### Markdown и формулы
- Полная поддержка Markdown
- Рендеринг LaTeX формул через KaTeX
- Подсветка синтаксиса кода
- Изображения с модальным просмотром

### Playground
- Интерактивная визуализация алгоритма градиентного спуска
- Настройка параметров (шаг обучения, начальное значение)
- Графики траектории и градиента
- Таблица итераций

## 🔧 Технологии

- **React 18** - UI библиотека
- **TypeScript** - типизация
- **Vite** - сборщик и dev-сервер
- **React Router** - маршрутизация
- **react-markdown** - рендеринг Markdown
- **KaTeX** - рендеринг математических формул
- **rehype-highlight** - подсветка кода
- **Recharts** - построение графиков

## 📱 Адаптивность

Приложение полностью адаптивно:
- На десктопе: боковое меню всегда видно
- На мобильных: меню скрывается в бургер-меню
- Все компоненты адаптируются под размер экрана

## 💾 Сохранение данных

Все настройки и прогресс сохраняются в `localStorage`:
- Размер шрифта
- Прогресс чтения по каждому разделу
- Последний открытый раздел

## 🎯 Роутинг

- `/` - Главная страница
- `/toc` - Содержание
- `/chapter/:chapterId/section/:sectionId` - Просмотр раздела
- `/playground` - Интерактивный playground
- `/glossary` - Глоссарий
- `/about` - О пособии

## 📚 Примеры использования

### Добавление новой главы

1. Откройте `src/data/chapters.ts`
2. Добавьте новый объект в массив `chapters`:

```typescript
{
  id: 'new-chapter',
  title: 'Новая глава',
  sections: [
    {
      id: 'section-1',
      title: 'Первый раздел',
      content: `# Ваш контент в Markdown`
    }
  ]
}
```

### Добавление изображения

1. Поместите файл в `public/images/`
2. В Markdown используйте: `![Alt text](/images/filename.jpg)`

### Добавление формулы

В Markdown:
- Блочная: `$$x = \frac{-b \pm \sqrt{b^2-4ac}}{2a}$$`
- Встроенная: `Формула $E = mc^2$ в тексте`

## 🐛 Решение проблем

### Изображения не отображаются
- Убедитесь, что путь начинается с `/` (например, `/images/photo.jpg`)
- Проверьте, что файл находится в папке `public/images/`

### Формулы не рендерятся
- Проверьте экранирование обратных слешей: `\\` вместо `\`
- Убедитесь, что формулы обернуты в `$...$` или `$$...$$`

### Ошибки при сборке
- Убедитесь, что все зависимости установлены: `npm install`
- Проверьте синтаксис TypeScript в файлах

## 📄 Лицензия

Образовательный проект.

---

**Удачной работы с пособием!** 🎓

