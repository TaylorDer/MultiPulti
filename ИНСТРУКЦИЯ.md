# 📖 Инструкция по добавлению контента

## 🎯 Быстрый старт

1. **Установите зависимости:**
   ```bash
   npm install
   ```

2. **Запустите проект:**
   ```bash
   npm run dev
   ```

3. **Откройте браузер:** `http://localhost:3000`

## 📝 Где добавлять ваш контент

### 1. Текст глав и разделов

**Файл:** `src/data/chapters.ts`

Отредактируйте массив `chapters`. Каждая глава содержит разделы с Markdown контентом.

**Пример:**
```typescript
{
  id: 'my-chapter',
  title: 'Моя глава',
  sections: [
    {
      id: 'section-1',
      title: 'Первый раздел',
      content: `# Заголовок

Ваш текст здесь. Поддерживается **жирный**, *курсив*.

## Подзаголовок

- Списки
- Работают
- Обычно

### Формулы LaTeX

Блочная формула:
$$\\int_{-\\infty}^{\\infty} e^{-x^2} dx = \\sqrt{\\pi}$$

Встроенная: $E = mc^2$

### Код

\`\`\`python
def example():
    return "Hello World"
\`\`\`
`
    }
  ]
}
```

**Важно для формул:**
- Используйте двойной обратный слеш: `\\` вместо `\`
- Блочные формулы: `$$...$$`
- Встроенные: `$...$`

### 2. Изображения

**Папка:** `public/images/`

1. Поместите изображения в эту папку
2. В Markdown используйте:
   ```markdown
   ![Описание](/images/your-image.jpg)
   ```

**Пример структуры:**
```
public/
  images/
    diagram-1.png
    graph-2.jpg
    formula-3.svg
```

### 3. Глоссарий

**Файл:** `src/data/glossary.ts`

Добавьте термины в массив `glossary`:

```typescript
{
  term: 'Оптимизация',
  definition: 'Процесс поиска наилучшего решения из множества возможных вариантов.'
}
```

## 🎨 Поддерживаемые возможности Markdown

### Заголовки
```markdown
# Заголовок 1
## Заголовок 2
### Заголовок 3
```

### Форматирование текста
```markdown
**жирный**
*курсив*
`код`
```

### Списки
```markdown
- Элемент 1
- Элемент 2

1. Нумерованный
2. Список
```

### Ссылки
```markdown
[Текст ссылки](https://example.com)
```

### Таблицы
```markdown
| Заголовок 1 | Заголовок 2 |
|-------------|-------------|
| Ячейка 1    | Ячейка 2    |
```

### Блоки кода
````markdown
```python
def function():
    pass
```
````

### Формулы LaTeX

**Блочные формулы:**
```markdown
$$
x = \frac{-b \pm \sqrt{b^2-4ac}}{2a}
$$
```

**Встроенные формулы:**
```markdown
Формула $E = mc^2$ в тексте.
```

**Важно:** В TypeScript строках используйте `\\` для обратного слеша.

## 📂 Структура файлов для контента

```
src/data/
  ├── chapters.ts    ← ВАШ ТЕКСТ ЗДЕСЬ
  └── glossary.ts    ← ВАШ ГЛОССАРИЙ ЗДЕСЬ

public/images/       ← ВАШИ ИЗОБРАЖЕНИЯ ЗДЕСЬ
```

## 🔄 Процесс добавления контента

1. **Откройте** `src/data/chapters.ts`
2. **Добавьте/отредактируйте** главы и разделы
3. **Поместите изображения** в `public/images/`
4. **Используйте** Markdown синтаксис для форматирования
5. **Сохраните** файл
6. **Проверьте** в браузере (автоперезагрузка работает)

## ✅ Чеклист перед публикацией

- [ ] Все главы добавлены в `chapters.ts`
- [ ] Все изображения помещены в `public/images/`
- [ ] Формулы правильно экранированы (`\\` вместо `\`)
- [ ] Глоссарий заполнен в `glossary.ts`
- [ ] Проект собирается без ошибок: `npm run build`
- [ ] Все ссылки на изображения начинаются с `/images/`

## 🐛 Частые проблемы

### Изображения не отображаются
- Проверьте путь: должен начинаться с `/images/`
- Убедитесь, что файл в папке `public/images/`
- Проверьте расширение файла (jpg, png, svg)

### Формулы не рендерятся
- Используйте `\\` вместо `\` в TypeScript строках
- Проверьте, что формулы обернуты в `$...$` или `$$...$$`

### Ошибки при сохранении
- Проверьте синтаксис TypeScript
- Убедитесь, что все строки правильно закрыты
- Проверьте запятые в объектах

## 📚 Дополнительная информация

Подробная документация в файле `README.md`

---

**Удачи в создании контента!** 🚀

